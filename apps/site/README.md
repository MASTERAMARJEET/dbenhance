# DBEnhance Site

EmDash CMS site deployed to Cloudflare Workers (D1 + R2).

See the [root README](../../README.md) for monorepo commands.

## Admin

After starting dev server, open `/_emdash/admin` to manage content.

## Deploy

```bash
pnpm deploy
```

From repo root: `pnpm deploy:site`
